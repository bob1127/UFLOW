---
description: 當使用者輸入 /change 時，切換本專案 Git 為 HTTPS + 瀏覽器驗證碼登入
alwaysApply: false
---

# /change — Git HTTPS + 瀏覽器驗證碼

當使用者訊息為 `/change` 或要求「切換 git https 驗證碼」「瀏覽器登入 github」時：

1. 請使用者在 **macOS「終端機」App**（非 Cursor 內建終端機）執行：`npm run change`
2. 腳本會啟動 `gh auth login -w`：顯示驗證碼 → 開啟 https://github.com/login/device → 用 bob1127 授權
3. 完成後執行 `npm run push` 推送
4. **不要**執行 `git config --global`，僅影響本專案 local 設定
5. 僅登出錯誤帳號 `kejiweibai17-source`，不影響其他專案

此指令會：
- 將 `origin` 設為 `https://github.com/bob1127/UFLOW.git`
- 清除 Keychain 舊憑證
- 透過 gh 瀏覽器裝置驗證碼登入
- 本專案 credential helper → `gh auth git-credential`
